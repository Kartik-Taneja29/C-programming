#include<stdio.h>
#define MAX 50
#define MIN 50
int main()
{
    int a[MAX], b[MIN];
    for(int i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i=0;i<5;i++)
    {
        scanf("%d",&b[i]);
    }
    
}